# Copy to .env and adjust before running docker compose up.
# Never commit the real .env file.

# --- MySQL ---
DB_NAME=defaultdb
DB_USERNAME=avnadmin
DB_PASSWORD=change-me-aiven-password
DB_PORT=3306
MYSQL_ROOT_PASSWORD=change-me-root

# --- Redis ---
REDIS_PORT=6379

# --- Backend (when run outside compose) ---
# DB_HOST=mysql-13abfe2a-connectly.i.aivencloud.com
# SERVER_PORT=8080
# CORS_ALLOWED_ORIGINS=http://localhost:5173

# JWT_SECRET is not needed until Phase 2 authentication.
# JWT_SECRET=
# JWT_ACCESS_EXPIRATION=15m
# JWT_REFRESH_EXPIRATION=7d
